<% if @knowledge_elements.errors.any? %>
  <div class="alert alert-danger">
    <strong><%= @knowledge_elements.errors.count %> Fehler gefunden:</strong>
    <ul>
    <% @knowledge_elements.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>