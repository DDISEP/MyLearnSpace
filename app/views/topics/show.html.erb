<div class="container-fluid">

  <div class="col-sm-9"><div class="row-fluid"><h1><%= @topic.name %></h1></div></div>

  <div class="col-sm-9">

    <div class="panel panel-primary">
      <div class="panel-body" id="knowledgeelements_list">
        <div class="row-fluid">

        </div>
      </div>

      <%= @topic.description %>

      </br>
      </br>
Zum Topic gehörige Lerneinheiten
      <table class="table table-striped">
        <thead>
        <tr><strong>
          <th width="20%"> Name der Lerneinheit </th>
          <th width="20%"> Beschreibung </th>
          <th>Übergreifendes Themengebiet</th>
        </strong></tr>
        </thead>

        <tbody>
        <% @knowledgeElements.each do |ke| %>
            <tr>
              <!-- show title, author, and creation date for each exercise -->
              <td> <%= link_to ke.name, ke %> </td>
              <td> <%= ke.description %></td>
              <td> <% if !ke.topic.nil?%><%= link_to ke.topic.name, ke.topic %><%end%></td>
              <% if @current_user.admin? %>
                  <td width="100">
                    <%= button_to 'Löschen', {:action => "destroy", :id => ke.id}, :confirm => 'Are you sure?', :method => "delete",
                                  :remote => true, :disable_with => 'loading...', class: "btn btn-xs btn-primary"%>
                    <%#= button_to ("löschen", :id => ke.id, :method => :delete, data: {confirm: "Are you sure?"},
                                               :remote => true, :disable_with => 'loading...'")
                    %></td>
                  <td width="100">
                    <%= button_to 'Bearbeiten',{:action => "edit", :id => ke.id}, :method => :get, class:"btn btn-xs btn-primary"%></td>
              <% end %>
            </tr>
        <% end %>
        </tbody>
      </table>
    </div>
  </div>


</div> <!-- /col-sm-9 -->
</div> <!-- /container-fluid -->


</div> <!-- /col-sm-9 -->


</div> <!-- /container-fluid -->






<td width="300"></td><%= button_to 'Zurück zur Übersicht', topics_path, :method => :get, class:"btn btn-xs btn-primary"%></td>
