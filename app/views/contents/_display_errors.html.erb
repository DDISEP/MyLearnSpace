<% if @content.errors.any? %>
  <div class="alert alert-danger">
    <strong><%= @content.errors.count %> Fehler gefunden:</strong>
    <ul>
    <% @content.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>