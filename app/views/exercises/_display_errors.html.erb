<% if @exercise.errors.any? %>
  <div class="alert alert-danger">
    <strong><%= @exercise.errors.count %> Fehler gefunden:</strong>
    <ul>
    <% @exercise.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>