<% if @item.errors.any? %>
  <div id="error_explanation">
    <strong><%= @item.errors.count %> Fehler gefunden:</strong>
    <ul>
    <% @item.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>