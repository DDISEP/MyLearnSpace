<% if @learningUnit.errors.any? %>
  <div class="alert alert-danger">
    <strong><%= @learningUnit.errors.count %> Fehler gefunden:</strong>
    <ul>
    <% @learningUnit.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
