<%= form_for(@peer_to_peer_message) do |f| %>
  <% if @peer_to_peer_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@peer_to_peer_message.errors.count, "error") %> prohibited this peer_to_peer_message from being saved:</h2>

      <ul>
      <% @peer_to_peer_message.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="container-fluid">
    <div class="panel panel-primary">
      <div class="panel-body">
        <div class="row-fluid">
          <div class="form-group">
            <div class="span1"><b><%= f.label :sender, "Absender" %></b></div>
            <div class="span6"><%= f.select :sender, options_for_select([current_user]), class: "form-control" %></div>
          </div>
        </div>
        <div class="row-fluid">
          <div class="span1"><b><%= f.label :reciever, "Empfänger" %></b></div>
          <div class="span6"><%= f.text_field :reciever, style:"width:40%; height:100%;" %></div>
        </div>
        <div class="row-fluid">
          <div class="span1"><b><%= f.label :subject, "Betreff" %></b></div>
          <div class="span11"><%= f.text_field :subject, style:"width:100%; height:100%;" %></div>
        </div>
        <div class="row-fluid">
          <div class="span1"><%= f.label :message, "Inhalt" %></div>
          <div class="span11"><%= f.text_area :message, style:"width:100%; height:80px; resize:vertical;" %></div>
        </div>
        <div id="send_button" class="row-fluid">
          <div class="span4 offset4"><%= f.submit "Nachricht senden", class:"btn btn-xs btn-success", style:"width:100%" %></div>
        </div>
      </div>
    </div>
  </div>


<% end %>

