<p id="notice"><%= notice %></p>

<p>
  <strong>Titel:</strong>
  <%= @materialtext.title %>
</p>

<p>
  <strong>Text:</strong>
  <%= @materialtext.text %>
</p>

<p>
  <strong>Autor:</strong>
  <%= @user_author_name %>
</p>

<table>
  <thead>
  <tr>
    <th></th>
  </tr>
  </thead>
  <tbody>
      <tr>
        <td><%= button_to 'Bearbeiten', edit_materialtext_path, {:method => "get", :id => @materialtext.id}%></td>
        <td><%= button_to 'Löschen', materialtext_path, {:method => "delete", :id => @materialtext.id}%></td>
        <td><%= button_to 'Zurück', materials_path, :method => "get"%></td>
      </tr>
  </tbody>
</table>

<!--
%= link_to 'Bearbeiten', edit_materialtext_path(@materialtext) %> |
%= link_to 'Löschen', materialtext_path, method: :delete, data: { confirm: 'Bist du sicher?' } %> |
%= link_to 'Zurück', materials_path %>
-->