<%= form_for(@materialtext) do |f| %>
  <% if @materialtext.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@materialtext.errors.count, "error") %> prohibited this materialtext from being saved:</h2>

      <ul>
      <% @materialtext.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
